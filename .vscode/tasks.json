{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Build(Debug)",
			"type": "shell",
			"command": "source ./tool/build.sh ${relativeFile}",
		},
		{
			"label": "Build(Release)",
			"type": "shell",
			"command": "source ./tool/build.sh ${relativeFile} R",
		},
		{
			"label": "Run",
			"type": "shell",
			"command": "ulimit -s 1048576 && ./a.out<in",
		}
	],
	// "inputs": [
	// 	{
	// 		"id": "src",
	// 		"description": "Source File:",
	// 		"default": "${file}",
	// 		"type": "promptString"
	// 	},
	// ]
}