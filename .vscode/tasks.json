{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "g++ ${relativeFile} && echo Build Succeed."
        },
        {
            "label": "Build(Debug)",
            "type": "shell",
            "command": "g++ -g -O0 -D DEBUG ${relativeFile} && echo 'Build(Debug) Succeed.'"
        },
        {
            "label": "Run",
            "type": "shell",
            "dependsOn": ["Build"],
            "command": "./a.out"
        }
    ]
}