{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++ -o b.exe -std=c++17 main.cpp && echo Build Succeed. && merger.exe main.cpp && echo Merge Succeed.",
            "problemMatcher": []
        },
        {
            "label": "build & run",
            "type": "shell",
            "command": "g++ -o b.exe -std=c++17 main.cpp && echo Build Succeed. && merger.exe main.cpp && echo Merge Succeed. && b.exe",
            "problemMatcher": []
        }
    ]
}