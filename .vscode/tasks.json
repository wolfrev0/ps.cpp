{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "g++ ${file} && merger.exe main.cpp && echo Build Succeed.",
            "problemMatcher": []
        },
        {
            "label": "Build(Debug)",
            "type": "shell",
            "command": "g++ -g -O0 -D DEBUG ${file} && merger.exe main.cpp && echo Build Succeed.",
            "problemMatcher": []
        },
        {
            "label": "Run",
            "type": "shell",
            "dependsOn": ["Build"],
            "command": "a.exe",
            "problemMatcher": []
        }
    ]
}