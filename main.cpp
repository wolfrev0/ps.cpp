#include "Core.h"

using namespace std;

int main(){
    ios::sync_with_stdio(false), cin.tie(nullptr), cout.tie(nullptr);
    cout << fixed << setprecision(3);
    srand((uint)time(0));
	
	cout<<"Hello!"<<endl;

	return 0;
}